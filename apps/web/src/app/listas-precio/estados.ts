/** Etiquetas del alcance de una lista de precios. */
export const ALCANCE_LISTA: Record<string, string> = {
  customer: 'Por cliente',
  channel: 'Por canal',
  general: 'General',
}

/** Etiquetas del estado de una lista de precios. */
export const ESTADO_LISTA: Record<string, string> = {
  active: 'Activa',
  inactive: 'Inactiva',
}
