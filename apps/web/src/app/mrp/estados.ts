/** Etiquetas del estado de una sugerencia de MRP. */
export const ESTADO_SUGERENCIA: Record<string, string> = {
  pending: 'Pendiente',
  accepted: 'Aceptada',
  dismissed: 'Descartada',
}

/** Etiquetas de la accion sugerida. */
export const ACCION_SUGERENCIA: Record<string, string> = {
  purchase: 'Comprar',
  produce: 'Producir',
}
