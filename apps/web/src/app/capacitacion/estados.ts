/** Etiquetas del estado de un curso. */
export const ESTADO_CURSO: Record<string, string> = {
  active: 'Activo',
  archived: 'Archivado',
}

/** Etiquetas del estado de una inscripcion. */
export const ESTADO_INSCRIPCION: Record<string, string> = {
  enrolled: 'Inscrito',
  completed: 'Completado',
  failed: 'No aprobado',
}
